from keras.models import load_model
 
# load model
model = load_model('/content/drive/My Drive/Waqas/hamza_drive_1_train/my_model_pretrain_VGG16.h5')
model.load_weights('/content/drive/My Drive/Waqas/hamza_drive_1_train/my_model_weight.h5')

image = cv2.imread('/content/drive/My Drive/examples/39.jpg')
#output = imutils.resize(image, width=400)
 
# pre-process the image for classification
image = cv2.resize(image, (96, 96))
image = image.astype("float") / 255.0
image = img_to_array(image)
image = np.expand_dims(image, axis=0)

prediction = model.predict(image)
y_classes = prediction.argmax(axis=1)
y_classes 